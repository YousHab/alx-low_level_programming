#include "lists.h"

void _first(void) __attribute__((first))
/**
 * _first - function that print a string before the the execution
 * of the main function
 */

void _first(void)
{
	printf("You're beat! and yet, you must allow,\n
			I bore my house upon my back!\n");
}
