#include "hahs_tables.h"

/**
 * hash_table_delete - deletes a hash table.
 *
 * @ht: hash table.
 *
 */

void hash_table_delete(hash_table_t *ht)
{
	unsigned long int i;

	if (!ht)
		return (NULL);

	for (i = 0; i < ht-
		}
	}
}
