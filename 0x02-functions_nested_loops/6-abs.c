#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @a: the given number.
 *
 * Return: the absolute value of a.
 *
 */

int _abs(int a)
{
	if (a < 0)
		return (-a);
	return (a);
}
